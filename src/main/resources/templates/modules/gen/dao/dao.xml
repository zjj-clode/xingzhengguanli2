<?xml version="1.0" encoding="utf-8"?>
<template>
	<name>dao</name>
	<filePath>src/main/java/${packageName}/${moduleName}/dao/${subModuleName}</filePath>
	<fileName>${ClassName}Dao.java</fileName>
	<content><![CDATA[
package ${packageName}.${moduleName}.dao<#if subModuleName != "">.${subModuleName}</#if>;
import java.util.List;
import com.thinkgem.jeesite.common.persistence.CrudDao;
import com.thinkgem.jeesite.common.persistence.annotation.MyBatisDao;
import ${packageName}.${moduleName}.entity<#if subModuleName != "">.${subModuleName}</#if>.${ClassName};

/**
 * ${functionName}DAO接口
 * @author ${functionAuthor}
 * @version ${functionVersion}
 */
@MyBatisDao
public interface ${ClassName}Dao extends CrudDao<${ClassName}> {
	long findCount(${ClassName} ${className});
	void batchInsert(List<${ClassName}> ${className});
	void batchUpdate(List<${ClassName}> ${className});
	void batchInsertUpdate(List<${ClassName}> ${className});
	void disable(${ClassName} ${className});
	void deleteByIds(${ClassName} ${className});
}]]>
	</content>
</template>